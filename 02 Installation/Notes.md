## Installation of NodeJS
1. go to nodejs.org
2. download lts (stable, recommended) or current (testing) version for your platform (windows, mac, linux)
3. open the setup and install it.
4. open a terminal and type node --version

- type node for running javascript using REPL